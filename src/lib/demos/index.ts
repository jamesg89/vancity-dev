import type { DemoConfig } from './types';
import { waterstcafe } from './waterstcafe';
import { alexjo } from './alexjo';

export const demos: Record<string, DemoConfig> = {
  waterstcafe,
  alexjo,
};
